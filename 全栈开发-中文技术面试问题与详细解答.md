# 全栈开发中文技术面试问题与详细解答

> 以下问题根据《萧嘉磊-全栈开发工程师-2026新版》中的工作经历和项目经历整理。回答时可以先说每题开头的总结，再根据面试官的追问展开细节。涉及最近项目的后端工作时，应明确说明自己是“参与接口设计与联调”，避免把团队成果全部描述成个人成果。

## 1. 请介绍一下你最近负责的微信小程序和后台管理系统，以及你在项目中的具体职责

**参考回答：**

这个项目包括面向会员的微信小程序和面向运营人员的后台管理系统，主要业务有会员权益、优惠券、业务配置、复杂表单和数据看板。小程序主要使用 TypeScript，管理后台使用 React、TypeScript、Ant Design 和 ECharts；服务端使用 Java 17、Spring Boot 3、Spring Security 6、MyBatis-Plus、MySQL 和 Redis。

我的角色是全栈开发工程师，但工作重点在前端。我主要负责需求和用户流程梳理、页面及交互设计、公共组件开发、接口联调、测试、上线和线上问题处理。项目期间累计交付了 100 多个页面和业务模块，系统服务 1 万多名会员用户。

在前端工作之外，我也参与了会员、优惠券和登录相关 Java 接口的设计与联调。例如，我会和后端确认请求参数、返回字段、状态码和异常情况，通过 SpringDoc OpenAPI 检查接口约定，并参与 JUnit 5 用例补充。我不会把自己描述成这个项目的后端负责人，但我能够理解完整调用链，并处理前后端之间的问题。

一次完整的业务请求通常是：用户在小程序或后台发起操作，前端先做输入校验和登录状态检查，然后请求后端接口；后端完成身份认证、权限判断、业务处理和数据库访问，最后返回统一结果。前端再根据结果显示成功、失败、空数据或权限不足等状态。

## 2. 你在 React 和 TypeScript 项目中是怎样设计可复用组件的？

**参考回答：**

我不会为了复用而过早抽象。通常先观察多个业务页面中重复出现的结构和行为，再决定哪些部分适合做成公共组件。在后台系统中，我主要封装过表单、筛选区域、弹窗和状态反馈等组件。

设计组件时，我会重点处理以下几个方面：

1. **职责清晰。** 一个组件尽量只解决一类问题。例如，筛选组件负责收集筛选条件，但请求数据和保存业务状态一般由页面或业务层负责。
2. **接口稳定。** 使用 TypeScript 定义 props、事件回调和接口数据类型，明确哪些参数必填、哪些参数可选，减少错误传参。
3. **支持常见状态。** 除了正常显示，还要考虑加载中、无数据、请求失败、禁用、只读和权限不足等情况。
4. **允许业务扩展。** 通用行为通过参数控制，特殊内容可以通过 children、插槽式区域或渲染函数传入，避免在公共组件中加入大量业务判断。
5. **控制抽象范围。** 如果两个页面只是看起来相似，但业务规则差异很大，我会保留独立实现，避免形成难以维护的“大而全”组件。

组件完成后，我会在真实业务页面中验证它是否容易使用，并检查类型、默认值、边界条件和旧页面兼容性。好的公共组件不仅减少代码，还应该让团队更容易理解和修改业务。

## 3. 你是怎样优化微信小程序首页加载速度和重复请求问题的？

**参考回答：**

我先从代码下载量和接口请求两个方向定位问题。原来一些非首屏业务页面也放在主包中，用户打开小程序时需要下载较多暂时用不到的代码。另外，首页多个模块可能请求相同或相近的数据，造成重复加载。

针对代码体积，我把非首屏、低频使用并且可以独立加载的页面拆到分包中，首屏只保留启动和首页必须使用的代码，其他页面进入时再按需加载。拆分时还要检查公共依赖的位置，避免同一个大依赖被多个分包重复打包。

针对重复请求，我做了请求合并和缓存优先处理。同一份数据在有效期内优先读取前端缓存；如果同一时刻已有相同请求正在执行，其他调用方复用这个请求结果，而不是再次发送请求。服务端也使用 Redis 缓存高频读取的数据，形成前端缓存和服务端缓存的多级处理。

缓存不能只考虑速度，还要考虑一致性。我会根据数据的变化频率设置缓存有效期；用户执行会修改数据的操作后，要更新或清除相关缓存；请求失败时不能把错误结果长期缓存。验证效果时，我会比较首屏代码下载情况、网络请求数量、重复请求是否消失以及页面的实际可用时间，而不会在没有监控数据时编造具体的性能提升比例。

## 4. 请说明 JWT、Redis 和 RBAC 在登录认证与权限控制中的作用

**参考回答：**

这三个部分解决的问题不同：JWT 用于表示用户身份和登录信息，Redis 可以辅助管理令牌状态和有效期，RBAC 用于决定某个角色能够访问哪些资源或执行哪些操作。

前端请求层会先检查本地是否有 JWT，以及令牌是否已经明显过期。如果没有令牌或本地判断已经过期，就清理登录状态并跳转到登录页。这个检查主要是改善用户体验，不能代替服务端安全校验，因为前端数据可以被修改。

服务端收到请求后，由 Spring Security 验证令牌是否合法，再根据用户、角色和权限进行最终鉴权。前端根据 RBAC 结果控制路由、菜单和按钮，只是减少用户误操作；真正的权限边界必须由服务端保证。

Redis 可以保存登录会话或令牌相关状态，用于令牌续期、主动退出和登录失败限制。例如，用户退出后可以清除对应状态，使旧令牌不能继续使用；连续登录失败达到限制时，可以短时间限制继续尝试。实现时还要处理令牌过期、账号被禁用、权限发生变化、多端登录和 Redis 数据失效等情况。

接口返回上，我会区分“未登录或令牌无效”和“已经登录但没有权限”。前者通常要求重新登录，后者应该提示用户没有操作权限，而不是统一显示普通请求失败。

## 5. 在 ECharts 数据看板中，你如何排查重复渲染、长列表和内存问题？

**参考回答：**

我会先确认问题来自数据请求、状态更新、组件渲染还是图表实例本身，避免只看到页面卡顿就直接改代码。

对于重复渲染，我会检查 Vuex 或组件状态是否被重复更新、计算属性是否每次都生成新的大对象，以及父组件变化是否让所有子组件重新执行。处理方式是拆分图表组件和数据转换逻辑，只在原始数据真正变化时重新计算，并只更新发生变化的图表配置。

对于重复请求，我会检查生命周期、路由切换、筛选条件监听和多个组件是否同时触发了同一个接口。确认原因后，可以合并请求、减少不必要的监听，或复用已经加载的数据。

对于图表内存问题，要确保每个图表容器只维护一个 ECharts 实例。组件销毁或页面切换时，需要解绑窗口事件并调用 `dispose` 释放图表实例。如果只是数据变化，通常复用原实例并调用 `setOption`，不重复初始化。

对于长列表，我会优先考虑后端分页或前端分页，避免一次渲染大量节点；如果业务必须展示大量数据，再考虑虚拟列表。判断优化是否有效时，可以使用浏览器性能工具观察请求次数、渲染耗时、内存变化和页面切换后是否仍保留旧实例。

## 6. React 和 Vue 有什么区别？你在项目中会怎样选择？

**参考回答：**

React 和 Vue 都适合组件化开发，我在真实项目中都使用过。它们最大的差别不只是语法，而是组织组件、更新状态和使用生态工具的方式。

React 更接近 JavaScript 或 TypeScript 的编程方式，通常使用 JSX、Hooks 和单向数据流。它比较灵活，适合复杂交互、跨端开发以及团队希望自行组合技术方案的项目。我在管理后台、医学系统和 React Native App 中使用过 React 相关技术。

Vue 的模板、计算属性和响应式机制比较直观，官方生态的整体配合也比较统一，团队可以较快建立一致的页面结构。我在数据分析系统中使用 Vue、Vuex、Element UI 和 ECharts 开发过复杂数据页面。

选型时我会看现有项目和团队，而不是只按个人喜好决定，主要考虑：团队经验、已有代码和组件库、项目复杂度、维护周期、跨端需求、第三方生态以及招聘和交接成本。如果是在已有系统中开发，我通常优先保持原技术栈的一致性。对于新项目，再通过小范围验证比较开发效率和维护成本。

## 7. 请介绍医学临床数据管理系统中的 Node.js、MongoDB 和动态表单设计

**参考回答：**

这个项目用于临床试验数据管理，我负责前端架构和核心模块，也参与 Node.js、Express、MongoDB 服务端接口开发。主要功能包括项目管理、进度跟踪、表单模板、临床数据展示、操作日志以及短信和 Email 通知。

在服务端，我使用 Express 组织路由和接口，通过 Mongoose 定义数据结构并完成筛选、排序、字段投影和分页。查询接口不会默认返回所有字段，而是根据页面需要选择字段，并限制分页大小，减少网络传输和数据库压力。输入参数要先校验，查询条件也要使用允许列表，不能直接把客户端传入的任意字段拼进查询。

表单编辑器的核心思路是把表单描述成 JSON 配置。每个字段包含类型、名称、标题、默认值、校验规则和显示顺序等信息，前端根据配置渲染不同组件。用户在编辑器中调整字段后，系统保存的是配置数据，而不是写死的新页面代码，因此可以支持不同的临床表单模板。

这类设计要特别注意配置版本和历史数据兼容。例如，修改字段名称或类型时，旧数据是否还能显示；删除字段是物理删除还是停用；服务端是否也按同一套规则校验。医疗数据比较敏感，所以还应重视权限、操作日志、数据校验和错误追踪。

## 8. 如果让你设计一个 Spring Boot 业务接口，你会怎样分层并处理校验、事务和异常？

**参考回答：**

我通常按照 Controller、Service 和数据访问层划分职责。Controller 负责接收请求、做基础参数校验并返回统一结果；Service 负责业务规则和事务；Mapper 或 Repository 负责数据库访问。这样可以避免把业务逻辑都写在 Controller 中，也更方便测试和维护。

请求参数会使用专门的 DTO 表示，并通过 Bean Validation 检查必填项、长度、格式和数值范围。参数格式正确不代表业务一定正确，所以像“优惠券是否存在、是否过期、用户是否有资格领取、是否已经领取”等规则仍然需要在 Service 中校验。

需要同时修改多张表或多个相关状态时，事务应放在 Service 层。只有全部操作成功才提交；发生符合条件的异常时回滚。还要避免事务范围过大，例如不要在长事务中执行不必要的远程调用。对于重复提交，可以根据业务使用唯一约束、业务状态判断或幂等标识来防止重复写入。

异常处理方面，可以使用统一异常处理器，把参数错误、业务错误、未登录、无权限和系统错误转换成清晰且稳定的响应。返回给客户端的信息不能暴露数据库或服务器内部细节，但日志中要保留请求标识和足够的上下文，方便定位问题。

接口完成后，我会用 SpringDoc OpenAPI 确认接口契约，并使用 JUnit 5 覆盖正常流程、参数错误、无权限、重复操作、空值和事务回滚等关键情况。

## 9. 你通常怎样定位和优化一条慢 SQL？

**参考回答：**

我会先确认问题确实在 SQL，而不是接口等待、网络、锁竞争或前端重复请求造成的。然后通过日志记录的请求条件和耗时还原问题，找到实际执行的 SQL 和参数。

拿到 SQL 后，我会使用数据库的执行计划查看访问方式、扫描行数、索引使用、连接顺序以及是否出现额外排序或临时结果。常见检查点包括：

1. 查询条件和关联字段是否有合适的索引；联合索引的字段顺序是否符合主要查询方式。
2. 是否返回了不需要的字段，能否避免 `SELECT *`。
3. 是否在循环中反复查询，形成类似 N+1 的问题，能否改成批量查询或一次关联查询。
4. 筛选条件是否对索引字段做了函数计算或隐式类型转换，导致索引难以使用。
5. 分页是否一次跳过大量数据；深分页时是否可以根据上一页的主键或排序字段继续查询。

优化后要使用相同或相近的数据量重新比较执行计划和耗时，并检查结果是否正确。索引也不是越多越好，因为索引会增加写入成本和存储空间。我会根据实际查询频率和业务价值决定是否增加索引，而不是只为一条偶发查询增加很多索引。

## 10. 你怎样使用 Codex 或 Claude Code，同时保证代码质量和安全？

**参考回答：**

我把 AI 工具当作提高分析和执行效率的助手，不把它当作最终决策者。使用前，我会先写清楚业务背景、目标、允许修改的范围、边界条件和验收标准，并让工具复述任务、列出可能影响的文件和调用链。这样可以尽早发现它是否理解错误。

执行时，我会把任务拆成单一、可验证的小步骤。例如先定位问题，再修改一个明确目标，最后补充测试，而不是一次要求它大范围重构。每一步都检查代码差异，特别关注权限、空值、异常分支、数据结构变化和是否修改了任务范围之外的文件。

完成后，我会根据项目执行 TypeScript 类型检查、ESLint、Jest、JUnit、Maven 构建和接口回归，并手动检查核心业务流程。AI 生成了测试也不代表一定正确，我还会确认测试是否真的覆盖需求，而不是只验证它自己生成的实现。

安全方面，我不会把密码、令牌、用户隐私数据或未授权的内部信息直接提交给外部工具。对于登录、权限、SQL、配置和依赖变更等高风险内容，会进行更严格的人工复核。最终代码是否合并和上线，仍然由开发者和团队负责。

这种方式的价值主要是加快代码库理解、调用链分析、重复性编码和测试补充，同时通过小步修改和自动化检查控制风险。

## 11. React 中什么是纯函数和纯组件？什么是高阶组件？它们有什么区别？

**参考回答：**

纯函数和高阶组件是两个不同层面的概念。纯函数强调函数执行过程是否可预测、是否产生副作用；高阶组件强调如何复用组件逻辑。高阶组件可以由纯函数实现，但二者不能画等号。

### 1. 什么是纯函数？

纯函数通常有两个主要特点：

1. **相同输入得到相同输出。** 只要传入的参数相同，函数的返回结果就应该相同。
2. **执行时不修改函数外部的数据。** 它不会直接修改全局变量、传入对象、DOM 或其他外部状态，也不会在计算结果的同时发送请求或写入本地存储。

例如，下面的函数是纯函数：

```ts
function getTotal(price: number, count: number) {
  return price * count;
}
```

下面的写法不是纯函数，因为它修改了函数外部的变量：

```ts
let total = 0;

function addPrice(price: number) {
  total += price;
  return total;
}
```

React 希望组件的渲染过程保持纯净。可以把函数组件理解成一个接收 props、state 和 context，并返回 JSX 的函数。在同样的输入下，它应该返回同样的界面描述。渲染期间不应该修改 props、state 或外部变量，也不应该直接执行请求、定时器和 DOM 操作。

```tsx
type UserNameProps = {
  firstName: string;
  lastName: string;
};

function UserName({ firstName, lastName }: UserNameProps) {
  return <span>{firstName} {lastName}</span>;
}
```

组件保持纯净不代表它不能交互，也不代表不能使用 state。用户点击、提交等操作应该放在事件处理函数中；需要与外部系统同步的操作可以放在 `useEffect` 中。关键是不要在渲染过程中产生副作用。

例如，不应该直接修改数组：

```tsx
// 错误：直接修改了原数组
items.push(newItem);
```

应该创建新数组：

```tsx
setItems(previousItems => [...previousItems, newItem]);
```

这种不可变更新方式可以让数据变化更容易追踪，也能让 React 的浅比较正常工作。

### 2. 什么是纯组件？

“组件保持纯净”和 React 的性能优化 API 需要区分：

- **组件保持纯净**是一条开发原则，即渲染结果只由 props、state 和 context 决定，渲染过程没有副作用。
- **`React.memo`**用于函数组件。当父组件重新渲染而该组件的 props 没有变化时，React通常可以跳过这次子组件渲染。
- **`PureComponent`**用于类组件。它会对新的 props、state 与旧值进行浅比较，从而减少没有必要的重新渲染。

函数组件通常可以这样使用 `memo`：

```tsx
import { memo } from 'react';

type MemberRowProps = {
  name: string;
  status: string;
};

const MemberRow = memo(function MemberRow({ name, status }: MemberRowProps) {
  return (
    <div>
      <span>{name}</span>
      <span>{status}</span>
    </div>
  );
});
```

`memo`是一种性能优化，不是正确性的保证，也不是所有组件都要使用。如果组件本身渲染很简单，比较 props 的成本可能和重新渲染差不多。只有在组件渲染成本较高、父组件更新频繁，并且 props 经常保持不变时，我才会优先考虑使用它。

浅比较还要求注意引用类型。如果父组件每次渲染都创建新的对象、数组或函数，即使内容一样，子组件看到的引用也发生了变化，`memo`可能无法跳过渲染。遇到经过性能分析确认的问题时，可以配合 `useMemo` 和 `useCallback` 保持必要的引用稳定，但不应该为了形式统一而到处使用这些 API。

### 3. 什么是高阶组件？

高阶组件通常简称 HOC。它不是一种特殊组件类型，而是一个函数：接收一个组件，返回一个增加了新能力的组件。

```tsx
type PermissionProps = {
  permissionCode: string;
};

function withPermission<P extends object>(
  WrappedComponent: React.ComponentType<P>
) {
  function PermissionComponent(props: P & PermissionProps) {
    const { permissionCode, ...restProps } = props;
    const hasPermission = usePermission(permissionCode);

    if (!hasPermission) {
      return <div>没有操作权限</div>;
    }

    return <WrappedComponent {...(restProps as P)} />;
  }

  PermissionComponent.displayName =
    `withPermission(${WrappedComponent.displayName || WrappedComponent.name || 'Component'})`;

  return PermissionComponent;
}
```

在后台管理系统中，高阶组件可以用于复用以下横切逻辑：

- 权限判断；
- 登录状态检查；
- 统一加载和错误状态；
- 页面访问记录或埋点；
- 向多个组件注入相同的数据或行为。

使用高阶组件时，我会注意以下问题：

1. **不要修改原组件。** 应该返回一个包装后的新组件，并把无关的 props 继续传给原组件。
2. **不要在另一个组件的渲染函数中临时创建 HOC。** 否则每次渲染都会产生新的组件类型，可能导致子树卸载和重新挂载。HOC 应该在组件外部创建。
3. **注意 props 命名冲突。** HOC 新增的 props 应该有清晰的名称和 TypeScript 类型。
4. **注意 ref 和静态属性。** ref 不会像普通 props 一样自动传递，原组件的静态属性也不一定自动复制，需要根据实际情况单独处理。
5. **给包装组件设置清晰的 `displayName`。** 这样在 React 开发工具和错误信息中更容易定位。

### 4. 高阶组件和 Hooks 应该怎样选择？

在现代函数组件中，如果目标只是复用状态和业务逻辑，我通常优先使用自定义 Hook，因为调用关系更直接，组件嵌套也更少。例如，权限读取可以封装成 `usePermission`，不同页面直接调用它。

如果需要给一批组件统一增加页面级包装行为，或者现有组件和第三方库本来就使用 HOC，高阶组件仍然很合适。例如，要求所有受保护页面先检查登录状态，再决定显示页面、加载状态或无权限提示，这类场景可以使用 HOC 统一处理。

简单总结：纯函数让组件行为更稳定、更容易测试；`memo`和`PureComponent`主要用于减少不必要的渲染；高阶组件用于包装组件并复用横切能力。实际项目中应该先保证代码正确和清晰，再通过性能分析决定是否增加缓存和渲染优化。

## 12. 请详细说明前端常见的缓存机制，以及如何保证缓存更新和数据一致性

**参考回答：**

前端缓存不是单一技术，而是分布在网络、浏览器、页面和应用代码中的多层机制。常见类型包括 HTTP 缓存、CDN 共享缓存、应用内存缓存、Web Storage、IndexedDB、Service Worker 的 Cache API，以及浏览器的页面往返缓存。选择缓存方式时，我会先确认缓存对象、有效期、一致性要求、数据量和安全级别。

### 1. HTTP 缓存

HTTP 缓存主要用于 HTML、JavaScript、CSS、图片、字体和接口响应。浏览器根据响应头判断资源能否保存、能保存多久，以及过期后是否需要向服务器验证。

前端面试中经常把 HTTP 缓存分成“强缓存”和“协商缓存”。这两个名称方便理解，但本质上都是标准 HTTP 缓存流程的不同阶段。

#### 强缓存或新鲜度缓存

资源仍在有效期内时，浏览器可以直接使用本地副本，不发送网络请求。常见响应头有：

- `Cache-Control: max-age=3600`：资源在 3600 秒内有效。
- `Cache-Control: public`：允许浏览器和 CDN 等共享缓存保存。
- `Cache-Control: private`：只允许用户自己的浏览器缓存，通常用于个性化响应。
- `Cache-Control: immutable`：表示资源在有效期内不会变化，适合带内容哈希的静态资源。
- `Expires`：使用一个绝对过期时间，是较早的机制；如果同时存在，通常优先使用 `Cache-Control`。

需要特别说明：

- `no-cache`不是“不允许缓存”，而是可以保存，但再次使用前必须向服务器验证。
- `no-store`才是要求不要保存响应，适合高度敏感或不应该落盘的数据。
- `max-age=0`通常表示资源立即过期，之后使用时需要重新验证。
- `s-maxage`主要控制 CDN、代理等共享缓存的有效期，通常会覆盖共享缓存中的 `max-age`。

浏览器开发工具中看到的 **memory cache** 和 **disk cache**，主要表示浏览器把 HTTP 缓存副本放在内存还是磁盘。内存速度更快但生命周期短，磁盘可以保存更久。具体放在哪里通常由浏览器决定，它们不是两套独立的业务缓存协议。

#### 协商缓存或重新验证

资源过期后不一定要重新下载完整内容。浏览器可以带上验证信息询问服务器，资源没有变化时服务器返回 `304 Not Modified`，浏览器继续使用本地内容。

常见方式有：

1. **ETag**：服务器为资源生成标识，浏览器下次通过 `If-None-Match` 发送旧标识。标识一致时返回 304。它可以比修改时间更准确。
2. **Last-Modified**：服务器返回资源的最后修改时间，浏览器下次通过 `If-Modified-Since` 发送。资源没有更新时返回 304。它实现简单，但时间精度和内容变化判断不如 ETag 灵活。

如果两种验证方式同时存在，服务器通常优先判断 ETag。协商缓存虽然可能没有响应正文，但仍然产生一次网络往返，因此速度通常慢于仍在有效期内的强缓存。

`Vary`响应头也很重要。它告诉缓存还要根据哪些请求头区分响应，例如 `Vary: Accept-Encoding`。如果接口会根据语言、编码或设备返回不同内容，却没有正确设置缓存键或 `Vary`，可能把一个用户或一种格式的数据错误地返回给另一个请求。

### 2. 静态资源版本和 CDN 缓存

CDN 会把静态资源放到离用户更近的节点。它属于共享缓存，能减少源站压力和网络距离。前端常用的发布策略是：

- HTML 文件使用较短缓存，或者设置为每次重新验证，确保用户能够尽快取得新版本入口。
- JavaScript、CSS、图片和字体文件名带内容哈希，例如 `app.a83f21.js`。
- 带哈希资源设置较长的 `max-age`并配合 `immutable`，因为内容变化后会生成新文件名。

这种方式不需要强制用户清理浏览器缓存。新 HTML 引用新的哈希文件，旧资源即使仍在缓存中也不会影响新版本。发布时要避免先删除旧资源再发布 HTML，因为仍在使用旧 HTML 的用户可能继续请求旧文件。更安全的顺序是先上传新资源，再更新入口，并让旧资源保留一段时间。

CDN 还要注意缓存键、查询参数、Cookie、请求头和主动刷新策略。包含用户私有数据的接口不能因为错误的 `public`配置而被共享缓存。

### 3. JavaScript 应用内存缓存

应用可以在模块变量、`Map`、状态管理工具或数据请求层中保存结果。内存缓存访问速度最快，适合当前页面频繁使用、重新获取成本较高的数据。例如字典数据、业务配置、已经加载的用户信息或列表查询结果。

一个比较完整的内存缓存通常要包含：

- **缓存键**：一般由接口地址、查询参数、用户身份和数据版本组成。
- **缓存值**：接口数据，也可以包含正在执行的 Promise，用于合并同一时刻的重复请求。
- **有效期 TTL**：超过时间后重新请求。
- **容量限制**：数据较多时可以使用 LRU 思路删除长时间未使用的数据。
- **更新和失效方法**：业务修改成功后更新缓存或删除相关缓存。

例如首页多个模块同时需要同一份会员数据时，可以让它们复用同一个正在执行的 Promise，避免同时发出相同请求。请求成功后保存结果；请求失败后要删除这个 Promise，避免后续请求一直复用失败状态。

还可以采用 **stale-while-revalidate** 思路：先显示已有缓存，让页面尽快可用，同时在后台请求最新数据；新数据返回后再更新界面。这种方式体验较好，但只适合允许短时间显示旧数据的场景，不适合余额、支付状态或关键权限等强一致数据。

### 4. Web Storage：localStorage 和 sessionStorage

`localStorage`和`sessionStorage`都是浏览器提供的键值存储，只能直接保存字符串。

- `localStorage`通常在浏览器关闭后仍然存在，适合保存用户偏好、非敏感配置和可以重新生成的少量数据。
- `sessionStorage`通常只在当前标签页会话中存在，不同标签页相互独立，适合保存一次页面会话中的临时状态。

它们的 API 是同步的，大量读写会阻塞主线程，所以不适合保存大量数据。保存对象时需要序列化和反序列化，还应同时保存版本号和过期时间，因为 Web Storage 本身没有 TTL。读取时如果数据过期、格式错误或版本不兼容，就应该删除并重新获取。

不能把敏感信息长期放入 JavaScript 可读取的存储中。只要页面发生 XSS，攻击脚本就可能读取其中的数据。登录方案应根据具体安全要求设计，不能简单认为把令牌放进 `localStorage`就是通用安全方案。

### 5. Cookie

Cookie 的主要作用是让浏览器在符合域名、路径和安全规则时自动把少量数据发送给服务器。它更常用于会话和身份状态，不适合保存大体积缓存，因为 Cookie 大小有限，而且每次请求自动携带会增加网络开销。

常见安全属性包括：

- `HttpOnly`：JavaScript 不能读取，可降低令牌被 XSS 脚本直接窃取的风险。
- `Secure`：只通过 HTTPS 发送。
- `SameSite`：限制跨站请求携带方式，帮助降低 CSRF 风险。
- `Expires`或`Max-Age`：控制 Cookie 生命周期。

Cookie、HTTP 缓存和 Web Storage 是不同机制。Cookie 会参与请求，Web Storage 默认不会自动发送给服务器，而 HTTP 缓存保存的是请求对应的响应副本。

### 6. IndexedDB

IndexedDB 是浏览器中的异步、事务型数据库，可以保存结构化数据、对象、文件或 Blob，容量通常也比 Web Storage 大。它适合离线数据、较大的业务数据、本地搜索索引和需要分批读取的内容。

它支持对象仓库、键、索引、游标和事务。与 `localStorage`相比，IndexedDB 不会因为一次较大的同步操作直接阻塞主线程，但 API 和数据版本管理更复杂。

使用时要考虑：

- 数据库版本升级和表结构迁移；
- 缓存记录的业务版本和过期时间；
- 多标签页同时读写；
- 用户退出后清理属于该用户的数据；
- 浏览器存储配额、隐私模式和系统空间不足时的数据清理；
- 本地数据与服务端数据冲突时，以哪一方为准。

对于医疗、账号和隐私数据，即使技术上可以存入 IndexedDB，也要先确认业务和安全要求，不能只因为容量大就直接保存。

### 7. Service Worker 和 Cache API

Service Worker 位于页面和网络之间，可以拦截请求，并通过 Cache API 保存 Request 和 Response。它常用于 PWA、离线访问和可控制的资源缓存。

常见缓存策略包括：

1. **Cache First**：先查缓存，没有再请求网络。适合文件名带哈希、很少变化的静态资源。
2. **Network First**：先请求网络，失败或超时后使用缓存。适合希望数据较新，但离线时仍要提供内容的页面。
3. **Stale While Revalidate**：先返回缓存，同时在后台更新。适合允许短暂旧数据的内容。
4. **Cache Only**：只读缓存，适合安装阶段已经确定保存的离线资源。
5. **Network Only**：只走网络，适合不能缓存的敏感请求或实时操作。

Service Worker 缓存需要显式管理版本。例如发布新版本时创建新的缓存名称，在激活阶段删除确认不再使用的旧缓存。删除旧缓存不能过早，否则仍由旧 Service Worker 控制的页面可能找不到资源。

还要避免无条件缓存所有请求。POST 等修改数据的请求、用户私有响应、支付结果和一次性验证码通常不应该按普通 GET 资源缓存。跨域不透明响应、错误响应和重定向也要谨慎判断。Service Worker 本身有安装、等待和激活生命周期，新代码不会保证在文件下载后立即接管所有已打开页面。

### 8. 浏览器页面缓存：bfcache

bfcache 是 back/forward cache，也就是浏览器的前进后退缓存。用户离开页面后，浏览器可能保存整个页面状态，包括 DOM 和 JavaScript 内存。用户点击后退或前进时，可以直接恢复页面，而不是重新创建页面。

bfcache 与 HTTP 缓存不同：HTTP 缓存保存网络资源，bfcache 保存的是一整个可恢复的页面。页面从 bfcache 恢复时，某些初始化逻辑不会像首次加载一样重新执行。因此可以监听 `pageshow`事件，并通过它的 `persisted`属性判断是否从 bfcache 恢复；离开页面时可使用 `pagehide`处理必要状态。

应尽量避免依赖 `unload`事件，因为它可能影响页面进入 bfcache，而且在部分场景下并不可靠。页面恢复后，还要判断登录状态、时间敏感数据或长连接是否需要重新确认。

### 9. 组件和路由级状态保留

某些框架可以保留已经创建的组件实例和页面状态。例如 Vue 的 `KeepAlive`可以缓存动态组件实例，保留表单、滚动位置和局部状态。它缓存的是组件实例，不是 HTTP 响应。

使用这类机制时，需要决定哪些页面可以保留、最多保留多少个实例，以及什么时候主动刷新或销毁。对于用户切换、权限变化和业务数据更新，不能因为页面被保留就一直显示旧状态。React 本身没有与 Vue `KeepAlive`完全相同的通用内置组件，项目通常通过路由结构、状态提升或专门的缓存方案实现类似需求。

另外，JavaScript 模块加载后也会由运行环境复用模块实例，同一个模块通常不会因为多次 `import`就重复执行初始化。这属于模块运行机制，不应该与业务接口缓存混为一谈。

### 10. preload、prefetch 和缓存的区别

`preload`和`prefetch`本身不是业务缓存策略，而是资源加载提示：

- `preload`提示浏览器当前页面很快会使用某个重要资源，应尽早加载。
- `prefetch`提示浏览器在空闲时加载将来可能使用的资源。

加载后的响应可能进入 HTTP 缓存，但是否保存以及保存多久仍然受 HTTP 缓存头和浏览器策略控制。滥用预加载会争抢首屏带宽，所以只应该预加载真正关键的资源。

### 11. 缓存一致性和失效策略

缓存最困难的部分通常不是“保存”，而是“什么时候更新”。我会根据业务采用以下方法：

1. **基于时间失效**：设置 TTL，到期后重新获取，适合允许短暂旧数据的内容。
2. **修改后失效**：新增、编辑或删除成功后，删除相关列表和详情缓存，下一次重新请求。
3. **修改后直接更新**：服务端返回最新对象后，直接更新详情和列表中的对应记录，减少一次请求。
4. **版本或内容哈希**：静态资源通过文件名哈希解决新旧版本问题；本地结构化数据通过 schema 版本处理迁移。
5. **服务端验证**：通过 ETag 或 Last-Modified 判断资源是否变化。
6. **按用户隔离**：缓存键包含用户或租户信息；退出登录和切换账号时清除私有缓存。
7. **容量淘汰**：通过 LRU、数量上限或存储配额处理长期不用的数据。
8. **请求去重**：同一时刻只执行一个相同请求，避免缓存失效后产生大量重复请求。

还要防止缓存穿透、击穿和雪崩。例如大量请求同时发现缓存过期时，可以复用同一个刷新 Promise，或者让过期时间带少量随机值，减少同一时间集中失效。前端做这些处理后，服务端仍然需要自己的保护，不能把稳定性完全依赖于客户端。

### 12. 结合项目说明我的使用方式

在微信小程序和后台项目中，我使用过前端缓存、请求合并以及服务端 Redis 多级缓存。首页优先读取仍然有效的数据，并合并同一时刻的重复请求；高频公共数据可以由 Redis 减少重复数据库查询。业务发生修改后，要清理或更新相关缓存，登录退出时要清理用户状态和私有数据。

如果是 Web 管理后台，我会让带内容哈希的 JavaScript 和 CSS 使用长期 HTTP 缓存，而 HTML 保持较短缓存或重新验证。对于字典和普通配置，可以使用带 TTL 的应用内存缓存；对于权限、当前账号和高实时性业务数据，则会更谨慎地重新验证。

简单总结：静态资源优先使用 HTTP 缓存、内容哈希和 CDN；页面运行期间的重复数据使用内存缓存和请求去重；少量非敏感偏好可以使用 Web Storage；大量结构化离线数据使用 IndexedDB；需要离线控制时使用 Service Worker；页面前进后退由 bfcache 优化。无论使用哪一层缓存，都必须同时设计缓存键、有效期、更新方式、容量限制、用户隔离和异常降级。

## 13. 请解释 JavaScript 事件循环、宏任务和微任务，并说明它们对页面性能有什么影响

**参考回答：**

JavaScript 主线程一次只能执行一个调用栈。同步代码先进入调用栈执行；定时器、网络请求和用户事件等异步工作由浏览器提供的能力处理，完成后再把对应回调放入任务队列。事件循环会在调用栈清空后选择合适的任务继续执行。

面试中通常把队列分为宏任务和微任务：

- 常见宏任务包括整段脚本、`setTimeout`、`setInterval`、用户交互和消息事件。
- 常见微任务包括 `Promise.then`、`Promise.catch`、`Promise.finally`和 `queueMicrotask`。

在一个宏任务执行结束后，浏览器会先清空当前产生的微任务，再考虑页面渲染和下一个宏任务。因此下面代码的输出顺序是 `A、D、C、B`：

```js
console.log('A');

setTimeout(() => console.log('B'), 0);

Promise.resolve().then(() => console.log('C'));

console.log('D');
```

`async/await`本质上也是基于 Promise。执行到 `await`时，函数后面的部分会暂停，并在 Promise 完成后作为后续微任务继续执行，但 `await`之前的代码仍然是同步执行的。

事件循环对性能的影响主要有两个方面：

1. 如果一个同步任务计算时间太长，主线程无法及时处理点击、滚动和渲染，页面就会卡顿。解决方式包括拆分任务、减少计算量、使用分页或虚拟列表，以及把适合的纯计算放到 Web Worker。
2. 微任务也不能无限增加。因为浏览器通常会先清空微任务，如果微任务不断产生新的微任务，渲染和用户事件同样可能被延迟。

在实际项目中，如果页面卡顿，我会通过浏览器 Performance 工具检查长任务、事件回调、脚本执行和渲染时间，而不是只看到异步代码就认为它不会阻塞主线程。网络请求虽然是异步的，但拿到大量数据后的转换和渲染仍然可能占用主线程。

## 14. TypeScript 给项目带来了什么价值？你如何使用 interface、type 和泛型？

**参考回答：**

TypeScript 的主要价值是在开发和构建阶段发现类型错误，并让组件、函数和接口之间的约定更清晰。它不能替代运行时校验，因为类型在编译后会被移除，服务端返回的数据仍然可能不符合前端定义。

我在 React、React Native 和微信小程序项目中主要用 TypeScript 处理以下内容：

- 定义组件 props、事件回调和状态类型；
- 定义接口请求参数和响应数据；
- 使用联合类型表达有限状态，例如 `'loading' | 'success' | 'error'`；
- 使用类型收窄处理不同的数据分支；
- 使用泛型封装请求结果、分页数据和公共组件。

`interface`和`type`都可以描述对象结构。我的使用习惯是：对于需要扩展、继承或对外公开的对象结构，可以优先使用 `interface`；对于联合类型、交叉类型、映射类型或类型组合，通常使用 `type`更自然。实际项目更重要的是团队保持一致，而不是认为其中一个在所有场景都更好。

```ts
interface Member {
  id: string;
  name: string;
  status: 'active' | 'disabled';
}

type ApiResult<T> = {
  code: number;
  message: string;
  data: T;
};

type PageResult<T> = {
  list: T[];
  total: number;
};

type MemberPageResult = ApiResult<PageResult<Member>>;
```

泛型的作用是让同一套结构支持不同数据类型，同时保留具体类型信息。例如 `ApiResult<T>`可以表示会员、优惠券或分页列表的接口结果，不需要为每一种数据复制相同结构，也不需要使用 `any`放弃检查。

我会尽量避免滥用类型断言和 `any`。如果外部数据类型未知，应该先用 `unknown`接收，再通过字段检查、类型保护或校验库确认结构。接口文档和 TypeScript 类型还要同步更新，否则类型只是开发者自己的假设。

## 15. React 中的 useEffect 应该怎样使用？常见问题有哪些？

**参考回答：**

`useEffect`主要用于让 React 组件与外部系统同步，例如网络连接、浏览器事件、定时器、第三方图表和手动 DOM API。它不应该成为所有业务逻辑的默认位置。如果一个值可以直接通过 props 和 state 计算得到，通常不需要再用 Effect 把结果保存成另一份 state。

一个 Effect 通常要考虑三个部分：建立同步、依赖变化后重新同步、组件卸载或下次执行前清理。

```tsx
useEffect(() => {
  function handleResize() {
    chart.resize();
  }

  window.addEventListener('resize', handleResize);

  return () => {
    window.removeEventListener('resize', handleResize);
  };
}, [chart]);
```

常见问题包括：

1. **依赖项不完整。** Effect 中读取了 props、state 或函数，却故意不写进依赖数组，容易得到旧值并产生闭包问题。应该调整代码结构，而不是直接关闭 lint 规则。
2. **依赖每次都变化。** 在渲染中创建的新对象或新函数会让 Effect 重复执行。可以把不需要响应式的数据移到组件外，或者在确有需要时稳定引用。
3. **没有清理副作用。** 事件监听、定时器、订阅和图表实例在组件卸载时不清理，可能造成内存泄漏和重复执行。
4. **请求竞态。** 用户快速切换筛选条件时，旧请求可能比新请求晚返回并覆盖最新结果。可以使用 `AbortController`取消旧请求，或通过请求标识忽略过期结果。
5. **无限更新。** Effect 修改了自己的依赖，依赖变化后又执行 Effect，形成循环。需要重新判断数据是否应该由 Effect 管理。

开发环境的 Strict Mode 可能额外执行一次“建立—清理—重新建立”流程，用于帮助发现清理不完整的问题。正确写好的 Effect 应该能够安全地重复建立和清理。

`useMemo`和`useCallback`与 `useEffect`用途不同。前两者主要用于缓存计算结果或函数引用，属于性能优化手段；`useEffect`用于组件渲染后与外部系统同步。它们都不应该在没有实际问题时机械使用。

## 16. 在 React、React Native 或 Vue 项目中，你如何选择状态管理方式？

**参考回答：**

我会先按照状态的使用范围和生命周期选择方案，而不是一开始就把所有数据放进 Redux 或 Vuex。

1. **组件局部状态**：只被一个组件使用的输入值、弹窗开关和临时交互状态，优先放在组件内部。
2. **状态提升**：几个相邻组件需要共享数据时，可以把状态提升到最近的共同父组件，通过 props 和回调传递。
3. **Context**：主题、语言、当前用户等被较多组件读取但变化不频繁的数据，可以使用 Context。高频更新的大对象直接放入一个 Context，可能让大量消费者重新渲染，需要拆分。
4. **Redux 或 Vuex**：用户信息、跨页面业务状态、训练记录、筛选条件等需要集中管理、更新规则较多或需要清晰追踪的数据，适合放入状态管理工具。
5. **服务端数据缓存**：接口列表和详情本质上来自服务端，不一定都要长期复制到全局状态。可以由专门的请求层或数据获取工具管理缓存、刷新和失效。

在健身追踪 App 中，我使用 Redux 管理用户、训练记录和目标，因为这些数据会被多个页面使用；登录前后的导航切换由 React Navigation 处理。在数据分析系统中，我使用 Vuex 管理跨组件筛选条件和共享数据，并统一加载、空数据和错误状态。

我会避免保存可以计算出来的重复状态。例如已经有完整列表时，如果选中项可以通过 `selectedId`查到，就不一定再保存一份完整的 `selectedItem`，否则两份数据容易不一致。选择状态管理工具的目标应该是让数据流更清楚，而不是让简单页面变复杂。

## 17. 你会怎样设计一个统一的前端请求层？

**参考回答：**

统一请求层的目标是处理各业务接口共有的行为，让页面只关心业务参数和结果。在 React Native 项目和管理系统中，我会集中处理基础地址、超时、登录信息、响应格式和错误类型。

常见设计包括：

1. 创建统一的 Axios 实例或请求客户端，设置基础地址、超时时间和公共请求头。
2. 请求前读取当前登录状态并注入令牌，但不能在模块初始化时只读取一次，否则登录变化后可能继续使用旧令牌。
3. 统一解析服务端响应，把网络失败、超时、未登录、无权限、业务错误和服务端异常区分开。
4. 统一处理 401，但要防止多个并发请求同时触发多次刷新令牌或多次跳转登录。可以让它们复用同一个刷新过程。
5. 支持取消请求。页面离开或筛选条件快速变化时，使用 `AbortController`取消旧请求或忽略过期响应。
6. 对完全相同的只读请求进行合并或短时间缓存，减少重复加载。
7. 记录必要的请求标识和耗时，方便前后端根据同一个标识排查问题。

重试也要谨慎。GET 等幂等请求在网络暂时失败时可以有限重试，并加入退避时间；新增订单、领取优惠券等写操作不能在没有幂等保护时自动重试，否则可能产生重复数据。

统一请求层不能把所有错误都弹出相同提示。请求层负责分类，页面仍然应该根据业务场景决定显示行内错误、空状态、重试按钮还是全局提示。对于可恢复错误，要给用户明确的下一步操作。

## 18. 前端常见的安全问题有哪些？如何防止 XSS、CSRF 和越权？

**参考回答：**

前端安全不能只靠前端实现。前端可以减少风险和改善交互，但身份认证、权限校验和关键数据验证必须由服务端完成。

### XSS

XSS 是把恶意脚本注入页面并在其他用户的浏览器中执行。React 默认会对 JSX 中的文本内容转义，但这不代表项目自动安全。使用 `dangerouslySetInnerHTML`、直接写入 DOM、加载不可信脚本或把用户输入拼进 URL 和 HTML 时仍然有风险。

主要措施包括：

- 默认按文本显示用户输入，不直接当作 HTML；
- 确实需要富文本时使用可靠的白名单清理工具；
- 避免执行动态字符串代码；
- 对输出位置进行正确编码；
- 配置内容安全策略 CSP，限制脚本来源；
- 管理第三方依赖和外部脚本；
- 不在 JavaScript 可读存储中长期保存不必要的敏感信息。

### CSRF

CSRF 是诱导已经登录的用户浏览器向目标站点发送非本人意愿的请求。它主要利用浏览器自动携带 Cookie 的行为。

常见防护包括 `SameSite` Cookie、CSRF Token、检查 `Origin`或 `Referer`，以及对重要操作进行再次确认。修改数据的接口不能使用 GET。即使使用 Token，也要防止 XSS，因为恶意脚本可能直接在站内发起合法形式的请求。

### 越权

前端隐藏菜单或按钮只能避免误操作，不能作为真正鉴权。攻击者可以绕过页面直接请求接口，因此服务端必须对每个受保护接口检查身份、角色、权限和数据归属。例如普通用户不能通过修改会员 ID 查看其他人的私有数据。

### CORS

CORS 是浏览器执行的跨域访问控制机制，需要由服务端返回允许的来源、方法和请求头。它不是身份认证，也不能阻止非浏览器客户端直接调用接口。带凭证的跨域请求不能简单配置任意来源，应该使用明确的允许列表。

我在登录和权限联调中会检查 401、403、令牌过期、账号禁用、路由权限和按钮权限，但最终都以服务端认证和鉴权结果为准。

## 19. 如果优惠券数量有限，同时有大量用户领取，你会如何防止重复领取和库存超卖？

**参考回答：**

这是一个高并发一致性问题。前端可以减少重复操作，但不能作为最终保证。即使按钮点击后立即禁用，用户仍然可以重复发请求、使用多个设备，或者因为网络重试产生重复操作，所以核心控制必须在服务端和数据库完成。

我会从以下几个层次设计：

1. **前端防重复提交**：点击领取后按钮进入加载状态，在结果返回前不再提交；同时为请求生成业务标识。这个措施主要改善体验和减少普通重复点击。
2. **用户领取唯一约束**：数据库对“优惠券活动 ID + 用户 ID”建立唯一约束。即使两个相同请求同时到达，也只能成功写入一次。
3. **原子扣减库存**：使用类似“库存大于 0 时才减 1”的条件更新，判断实际影响行数。不能先查询库存，再在没有锁或条件的情况下单独更新，否则两个请求可能同时看到相同库存。
4. **事务控制**：扣减库存和生成用户优惠券记录需要保持一致。发生异常时要回滚，或通过可靠的状态和补偿机制处理。
5. **幂等处理**：客户端重试同一个业务操作时，服务端通过幂等键返回第一次处理结果，避免重复创建记录。
6. **Redis 的使用**：流量很大时，可以用 Redis 原子操作或 Lua 脚本做预扣减和限流，但还要设计 Redis 与数据库之间的一致性、失败补偿和数据恢复。数据库仍要有最后的唯一约束和正确性保护。

还要考虑活动开始瞬间的大量请求、缓存过期、服务重启、消息重复消费和请求超时后用户再次点击等边界情况。上线后需要监控成功数、失败原因、剩余库存和数据库记录数量，及时发现不一致。

结合我的经历，我参与过会员、优惠券接口设计与联调，也处理过前端防重复和异常状态。如果面试官问到完整的高并发方案，我会说明哪些是我实际参与的内容，哪些是我根据系统设计原则给出的方案，不会把未实际负责的服务端架构说成个人成果。

## 20. 你会怎样为前端和后端设计测试策略？

**参考回答：**

测试应该围绕风险和业务结果设计，而不是单纯追求用例数量。我会把测试分为不同层次，让问题尽量在成本较低的阶段被发现。

### 前端测试

- 对数据转换、表单规则和状态更新等纯逻辑写单元测试。
- 对 Redux 的状态变化、关键 Hooks 和公共组件验证输入与输出。
- 对页面检查加载、成功、空数据、失败、无权限和重复提交等状态。
- 对关键用户流程做集成测试或人工回归，例如登录、领取优惠券、编辑配置和保存表单。
- 使用 TypeScript 类型检查、ESLint 和构建命令发现类型、规范和打包问题。

在 React Native 健身 App 中，我使用 Jest 验证 Redux 状态更新和核心数据转换逻辑。对于 FlatList 性能相关改动，除了单元测试，还要在真实设备或接近真实数据量的环境中观察滚动和渲染情况。

### 后端测试

- Service 层单元测试覆盖主要业务规则、边界值和异常分支。
- 接口测试检查参数校验、响应结构、身份认证和权限。
- 数据库相关测试验证唯一约束、事务提交、回滚和并发修改。
- 外部短信、Email 等服务应通过模拟或测试环境验证，避免单元测试真正发送消息。

在 Java 项目中，我使用或参与补充 JUnit 测试，重点检查正常流程、空值、重复操作、权限和异常情况。Node.js 项目中使用过 Mocha 验证核心模块和异常分支。

发布前，我会根据本次改动范围制定回归清单。修复一个缺陷时最好补充能够复现该缺陷的测试，避免以后再次出现。测试环境通过不代表线上一定安全，还要确认配置、数据库变更、缓存和发布顺序。

## 21. 线上系统出现问题时，你如何从前端到后端定位原因？

**参考回答：**

我会先控制影响，再定位原因。第一步确认问题的范围和严重程度：是一个用户、一个功能、一个版本还是所有用户；是显示错误、数据错误、性能下降还是完全不可用；是否涉及权限、资金或敏感数据。

然后收集可以还原问题的信息，包括用户操作步骤、账号和权限范围、发生时间、页面地址、客户端版本、错误截图和请求标识。没有完整上下文时，不应该只凭一条错误信息猜原因。

排查顺序通常是：

1. **前端页面**：查看控制台错误、资源是否加载、状态是否正确、最近是否修改了相关代码。
2. **网络请求**：检查请求地址、参数、请求头、状态码、响应内容和耗时，确认请求是否重复或被取消。
3. **服务端日志**：根据时间、用户和请求标识查找完整调用链，确认异常发生在认证、业务逻辑、数据库还是外部服务。
4. **数据层和缓存**：检查数据库记录、SQL、事务状态和 Redis 数据，判断是否有脏数据、过期数据或缓存不一致。
5. **环境和发布**：比较线上配置、依赖、数据库脚本和最近发布差异，确认问题是否只存在于某个环境。

如果影响较大并且可以安全回退，我会先回滚版本、关闭有问题的功能或使用降级方案恢复核心服务，再继续分析。修复后要在接近线上条件的环境中验证，并回归相关功能，而不是只验证报错页面。

最后应记录根因、影响范围、修复方式和预防措施。例如补充日志、监控、自动化测试、发布检查或数据校验。我的项目经历中处理过前端重复请求、图表实例未释放、登录状态、接口异常和数据问题，这些都需要从页面、接口和数据链路一起判断。

## 22. 如果让你设计一个可维护、可扩展的运营管理系统，你会怎样考虑整体架构？

**参考回答：**

我不会一开始就追求复杂架构，而是先明确用户、业务规模、核心流程、权限模型、数据量、可用性目标和团队能力。架构的目标是支持业务和长期维护，不是使用尽可能多的技术。

### 前端设计

前端可以按基础设施、公共组件、业务模块和页面进行分层。请求层统一处理认证、超时和错误；公共组件处理表单、筛选、表格、弹窗和状态反馈；业务模块按会员、优惠券、配置和报表等领域组织，避免所有代码都放在一个目录或全局状态中。

路由和按钮显示根据权限结果处理，但服务端仍做最终鉴权。接口类型通过 TypeScript 约束，并与 OpenAPI 契约保持一致。大模块采用路由级拆分和按需加载，数据看板注意图表实例、长列表和大数据转换性能。

### 后端设计

后端按接口层、业务层和数据访问层划分职责。业务较小时可以先使用结构清晰的模块化单体，避免过早拆分微服务；当不同模块在发布、扩容和团队边界上确实需要独立时，再考虑服务拆分。

接口需要统一参数校验、异常响应、认证、RBAC 权限和操作日志。重要写操作要考虑事务、幂等和并发控制。数据库根据查询方式设计索引，避免循环查询和返回无关字段。Redis 用于适合缓存的高频数据，但要同时设计失效和一致性。

### 稳定性和交付

系统应该有结构化日志、请求标识、关键指标和错误告警，方便快速发现并定位问题。测试覆盖核心业务和异常分支，发布前执行类型检查、ESLint、JUnit、构建和接口回归。Docker 可以帮助统一环境和复现问题，发布时还要准备数据库变更顺序、回滚方案和必要的数据备份。

### 安全和扩展

所有输入都需要校验，敏感操作由服务端鉴权，并记录必要的操作日志。用户私有数据不能错误地进入共享缓存。随着用户量增加，可以从静态资源 CDN、请求合并、缓存、数据库索引、读写热点和服务横向扩展等方向逐步优化，并通过真实监控数据决定瓶颈在哪里。

结合我的经历，我能够主责前端的信息架构、组件和交互，也能参与 Java 或 Node.js 接口、数据库、认证、测试和排障。面对更复杂的后端架构，我会和后端及运维人员共同设计，并清楚说明自己的负责范围。
